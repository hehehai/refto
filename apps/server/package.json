{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "hono": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/openapi": "catalog:",
    "@orpc/zod": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@refto-one/env": "workspace:*",
    "@refto-one/api": "workspace:*",
    "@refto-one/auth": "workspace:*",
    "@refto-one/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "tsdown": "^0.16.5",
    "alchemy": "catalog:",
    "wrangler": "^4.56.0",
    "@types/node": "catalog:",
    "@cloudflare/workers-types": "catalog:"
  }
}
